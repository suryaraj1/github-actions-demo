name: Shell Commands Workflow

on: [push]

jobs:
    run-shell-command:
      runs-on: ubuntu-latest
      steps:
        - name: echo a string
          run: eccho "hello world"
        - name: multiline script
          run: |
            node --version
            npm --version